<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar"
        tools:targetApi="31">
        <activity android:name=".usuarios.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".menu.MenuActivity" />
        <activity android:name=".clientes.ClientesActivity" />
        <activity android:name=".clientes.AgregarClienteActivity" />
        <activity android:name=".pedidos.PedidosActivity" />
        <activity android:name=".pedidos.AgregarPedidoActivity" />
        <activity android:name=".productos.AgregarProductoActivity" />
        <activity android:name=".productos.ProductosActivity" />
        <activity android:name=".facturas.FacturasActivity" />
        <activity android:name=".facturas.AgregarFacturaActivity" />
        <activity android:name=".usuarios.UsuariosActivity" />
        <activity android:name=".usuarios.AgregarUsuarioActivity" />
    </application>

</manifest>